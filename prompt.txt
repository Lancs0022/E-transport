J'aimerais avoir une formulaire qui occupe la moitie de ma page et qui a un gradient allant d'un bleu claire a un bleu fonce comme fond et est transparent. Je veux que ce fond et les zones de saisie du formulaires aient des bords arrondis. Voici les labels prevus pour les zones de saisie : Pseudo, Telephone, Mot de Passe (a cacher), la confirmation du mot de passe, et en fin un bouton submit pour envoyer les informations qui eux sont envoye par le formulaire avec la methode post et action sur le fichier : "accountScript.php".
Tous les champs des formulaires sont requises.
En parallele, je veux que ce code php utilise PDO pour se connecter a une base de donnees existante avec les parametres suivantes :
$bdd = "e-transport";
$DB_DSN = 'mysql:host=localhost;dbname=' .$bdd;
$DB_USER = 'root';
$DB_PSWD = '';
$option =
[
    PDO::MYSQL_ATTR_INIT_COMMAND => 'SET NAMES utf8',
    PDO::ATTR_ERRMODE => PDO::ERRMODE_WARNING,
    PDO::ATTR_EMULATE_PREPARES => false
];
Une premiere fonction insere les donnees dans la table
"$requete" contiendra l'objet PDO utilisant la methode prepare pour preparer l'insersion des elements dans la table client et bindParam pour affecter les valeurs prepares. htmlspecialchars() sera utilise sur les $_POST pour securiser l'entree.

Une deuxieme page sert de log in pour les clients. Ils doivent envoyer leurs pseudo et leurs mot de passe pour pouvoir se connecter. On cherchera dans la table si le nom et le mot de passe fournie sont corrects en utilisant PDO avec les memes predispositions que precedemment (methode prepare puis bind param).

Une fois le client connecte, il est directement redirige vers une page, une nouvelle formulaire pour remettre une reservation sur le site
